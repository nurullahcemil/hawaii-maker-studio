{
    "sections": {
      "main": {
      "type": "main-product",
      "blocks": {
        "vendor": {
          "type": "text",
          "settings": {
            "text": "{{ product.vendor }}",
            "text_style": "uppercase"
          }
        },
        "title": {
          "type": "title"
        },
        "price": {
          "type": "price"
        },
        "variant_picker": {
          "type": "variant_picker"
        },
        "quantity_selector": {
          "type": "quantity_selector"
        },
        "buy_buttons": {
          "type": "buy_buttons"
        },
        "description": {
          "type": "description"
        }
      },
      "block_order": [
        "vendor",
        "title",
        "price",
        "variant_picker",
        "quantity_selector",
        "buy_buttons",
        "description"
      ]
    },
    "event_info": {
      "type": "text-with-images",
      "blocks": {
        "text": {
          "type": "text",
          "settings": {
            "text": "{% if product.metafields.custom.event_handle != blank %}{% assign event = shop.metaobjects.event[product.metafields.custom.event_handle.value] %}{% if event %}{% if event.description != blank %}<h3>Description</h3><p>{{ event.description }}</p>{% endif %}{% if event.skill_level != blank %}<h3>Skill Level</h3><p>{{ event.skill_level }}</p>{% endif %}{% if event.audience != blank %}<h3>Who is it for</h3><p>{{ event.audience }}</p>{% endif %}{% if event.duration != blank %}<h3>Duration</h3><p>{{ event.duration }}</p>{% endif %}{% if event.what_to_expect != blank %}<h3>What to Expect</h3><p>{{ event.what_to_expect }}</p>{% endif %}{% if event.location != blank %}<h3>Location</h3><p>{{ event.location }}</p>{% endif %}{% if event.host_name != blank %}<h3>Host</h3><p>{{ event.host_name }}</p>{% endif %}{% if event.host_bio != blank %}<h3>About the Host</h3><p>{{ event.host_bio }}</p>{% endif %}{% endif %}{% endif %}"
          }
        }
      },
      "block_order": ["text"]
    }
  },
    "order": [
      "main",
    "event_info"
  ]
}